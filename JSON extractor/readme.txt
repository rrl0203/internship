This program takes as input the miners.json file from the minesweeper github, iterates through all of these URLs and invokes the minesweeper algorithm on it.
This program uses the json-simple library, which you may have to add to your classpath in your IDE.
